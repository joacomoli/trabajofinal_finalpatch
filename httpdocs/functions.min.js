


function login() {
  var passWord=prompt("","")
  if (passWord =="sapere aude") {
    href="/userspage/index.html"
  //url = 'userspage/index.html'
  window.open(url);//should let you download file.
  }
  else {
  window.close;//should close browser?
  };
}
/*  var username = prompt("Escriba su contraseña","25 inutiles");
  if (username == null || username == "" || username !== "25 inutiles") {
    prompt("usuario incorrecto");

  }else {
    window.location(userspage\index.html);
  };
    username;
    */
    function validar() {
  nombre = document.getElementsByName('nombre').value;
  apellido = document.getElementsByName('apellido').value;
  correo = document.getElementsByName('correo').value;
  if (password =="sapere aude") {

  }
else {
  false;
  location("index.html");
}
}
